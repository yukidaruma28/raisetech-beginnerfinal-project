# ユースケース

> メインドキュメント: [要件定義書](requirements.md)

## アクター

| アクター | 説明 |
|---------|------|
| ユーザー | このアプリを利用する唯一の人物（シングルユーザー） |

---

## ユースケース一覧

| ID | ユースケース名 | 関連機能 | MVP 実装状況 |
|----|-------------|---------|---|
| UC-01 | ボードを閲覧する | I-2 | ✅ 表示のみ実装済み（リストビュー）|
| UC-02 | 問い合わせを作成する | I-1 | ✅ 実装済み（ヘッダー右上「+ 新規」モーダルから作成）|
| UC-03 | 問い合わせ詳細を編集する | I-3 | ✅ 実装済み（行クリックで詳細モーダル / inline-edit + auto-save）|
| UC-04 | 問い合わせを削除する | I-4 | ✅ 実装済み（編集モーダル右上の削除ボタン → 確認ダイアログ）|
| UC-05 | 問い合わせをDnDで移動する | I-5 | ⏳ 未実装 |
| UC-06 | ステータスを作成する | S-1 | ⏳ 未実装 |
| UC-07 | ステータスを編集する | S-2 | ⏳ 未実装 |
| UC-08 | ステータスを削除する | S-3 | ⏳ 未実装 |
| ~~UC-09~~ | ~~優先度を作成する~~ | ~~P-1~~ | ❌ **MVP スコープ外** |
| ~~UC-10~~ | ~~優先度を編集する~~ | ~~P-2~~ | ❌ **MVP スコープ外** |
| ~~UC-11~~ | ~~優先度を削除する~~ | ~~P-3~~ | ❌ **MVP スコープ外** |
| ~~UC-12~~ | ~~ラベルを作成する~~ | ~~LB-1~~ | ❌ **MVP スコープ外** |
| ~~UC-13~~ | ~~問い合わせにラベルを付与する~~ | ~~LB-2~~ | ❌ **MVP スコープ外** |
| ~~UC-14~~ | ~~問い合わせからラベルを解除する~~ | ~~LB-3~~ | ❌ **MVP スコープ外** |
| ~~UC-15~~ | ~~ラベルを削除する~~ | ~~LB-4~~ | ❌ **MVP スコープ外** |

> UC-12〜15（Label）と UC-04 の `category` / `assignee` 編集は MVP では実装しない（`docs/tech-stack.md` の「採用しないことを明示する技術」参照）。
> 優先度は **3 段階固定運用（高/中/低）**。`level` は 1..3 の UNIQUE 制約で常に 3 件、`inquiries.priority_id` は NOT NULL（デフォルト「低」）。UI からの追加・編集・削除は不要のため UC-09〜11 を MVP スコープ外とする。

---

## ユースケース詳細

### UC-01: ボードを閲覧する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | アプリが起動している |
| 事後条件 | ボード画面にすべてのステータス列・問い合わせ・プロパティが表示されている |

**基本フロー**
1. ユーザーがブラウザでアプリにアクセスする
2. システムが `GET /api/inquiries` を呼び出す
3. システムがスケルトン表示でローディング状態を示す
4. データ取得完了後、ボード画面にステータス列と問い合わせカードを表示する

**代替フロー**
- 2-a: データ取得に失敗した場合、エラートーストを表示する

---

### UC-02: 問い合わせを作成する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | ボード画面が表示されている、ステータスが1件以上存在する |
| 事後条件 | 新しい問い合わせが指定したステータス列の末尾に追加されている |

**基本フロー**
1. ユーザーが画面右上ヘッダーの「+ 新規」ボタンをクリックする
2. 問い合わせ作成モーダルが開く（ステータスは先頭列、優先度は「低」が初期選択）
3. ユーザーがタイトル（必須）・本文・ステータス・優先度を入力する
4. ユーザーが「作成」ボタンをクリックする
5. システムが `POST /api/inquiries` を呼び出す
6. モーダルが閉じ、新しい問い合わせが該当ステータス列の末尾に表示される

**代替フロー**
- 3-a: タイトルが空の場合、フィールド下部に「タイトルは必須です」を表示し送信を阻止
- 2-a: ×ボタン・オーバーレイクリック・Escキーでモーダルを閉じる
- 5-a: サーバが 422 を返した場合、`details` をフィールド単位の赤帯と各 input のリングで表示しモーダルは閉じない

> **MVP スコープ外（後続 Issue へ）**
> - 各ステータス列下部の「＋ 追加」ボタンは未実装。ヘッダー「+ 新規」のみが起点。
> - category / assignee / labels の入力欄は提供しない（`docs/tech-stack.md` 参照）。
> - 楽観的更新は導入しない（UC-05 DnD 移動で初導入予定）。

---

### UC-03: 問い合わせ詳細を編集する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | 問い合わせが1件以上存在する |
| 事後条件 | 問い合わせの内容（タイトル・本文・カテゴリ・担当者・優先度・ラベル）が更新されている |

**基本フロー（Trello / Linear 風 inline-edit + auto-save）**
1. ユーザーが問い合わせ行をクリックする（行全体がクリック判定。checkbox / 優先度アイコンは除外）
2. InquiryEditDialog が開き、現在の内容が表示される
3. タイトル / 本文をクリック → その場で input / textarea に切り替わる
4. フィールドの外をクリック（blur）→ 値が変わっていれば `PATCH /api/inquiries/:id` をフィールド単位で送信
5. ステータス / 優先度の select は変更（change）と同時に即 PATCH
6. リスト側の表示が `invalidateQueries(['inquiries'])` で更新される

**代替フロー**
- 3-a: フィールド編集中に Esc → 編集前の値に戻して閲覧モードに復帰（PATCH しない）
- 4-a: タイトル空 / 256 文字超など zod 検証で弾かれた場合、編集モードを維持しフィールド下に赤エラー表示
- 4-b: サーバ 422 が返った場合、`details` をフィールド下にインライン表示し編集モード維持
- 2-a: ×ボタン / オーバーレイクリック / Esc（編集していないとき）でモーダルを閉じる
- 「保存」ボタンは持たない（auto-save なので未保存状態が発生しない）

> **MVP スコープ外（後続 Issue へ）**
> - 削除ボタン（UC-04 で詳細モーダル内に追加予定）
> - status_id 変更時の `position` 再計算は UC-05（DnD 移動）で対応。今回は status を変えても position は据え置き。
> - 楽観的更新は導入しない（UC-05 で初導入予定）。

---

### UC-04: 問い合わせを削除する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | InquiryModal が開いている |
| 事後条件 | 問い合わせが削除され、InquiryModal が閉じている |

**事前条件**: 行をクリックして InquiryEditDialog が開いている

**基本フロー**
1. ユーザーが編集モーダル右上のゴミ箱ボタン（「削除」）をクリックする
2. 確認ダイアログが編集モーダル上にスタックして表示される（「TASK-N を削除しますか？」）
3. ユーザーが「削除」を選択する
4. システムが `DELETE /api/inquiries/:id` を呼び出す（成功 204）
5. 確認ダイアログと編集モーダルが両方閉じ、リストから該当行が消える（`invalidateQueries(['inquiries'])`）

**代替フロー**
- 3-a: 「キャンセル」/ Esc / オーバーレイクリックで確認ダイアログを閉じる（削除しない、編集モーダルは開いたまま）
- 4-a: サーバが 404 を返した場合（他クライアントで先に削除済み）、目的達成として両モーダルを閉じてリスト再取得
- 4-b: ネットワーク障害などその他のエラーは編集モーダル上部の赤帯で通知（モーダルは開いたまま）

---

### UC-05: 問い合わせを DnD で移動する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | 問い合わせが1件以上存在する |
| 事後条件 | 問い合わせが指定したステータス列・位置に移動し、ステータスと position が更新されている |

**基本フロー**
1. ユーザーが問い合わせカードをドラッグする
2. ドラッグ中、ドロップ先にプレースホルダーが表示される
3. ユーザーが目的のステータス列・位置にドロップする
4. システムが `PATCH /api/inquiries/{id}/move` を呼び出す（新しい status_id と position）
5. 問い合わせが新しい位置に表示される

**代替フロー**
- 3-a: 元の位置にドロップした場合、位置は変わらず API は呼び出さない

---

### UC-06: ステータスを作成する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | 設定画面、またはボード画面のステータス追加ボタンが利用可能 |
| 事後条件 | 新しいステータス列がボードの末尾に追加されている |

**基本フロー**
1. ユーザーがボード右端の「＋ステータスを追加」ボタンをクリックする
2. ステータス作成フォームが表示される（名前・色を入力）
3. ユーザーが名前と色を入力して作成ボタンを押す
4. システムが `POST /api/statuses` を呼び出す
5. 新しいステータス列がボードの末尾に表示される

**代替フロー**
- 3-a: 名前が空の場合、作成ボタンを非活性にする
- 1-a: ステータス数が上限（20件）に達している場合、追加ボタンを非表示にする

---

### UC-07: ステータスを編集する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | ステータスが1件以上存在する |
| 事後条件 | ステータスの名前・色・並び順が更新されている |

**基本フロー**
1. ユーザーがステータス列のヘッダー（名前部分）をクリックする
2. 名前がインライン編集モードに切り替わる（色変更は別メニュー）
3. ユーザーが新しい名前を入力し Enter を押す
4. システムが `PATCH /api/statuses/{id}` を呼び出す
5. ステータス名が更新された状態で表示される

**代替フロー**
- 3-a: 名前が空の場合、保存せず元の名前に戻す
- 3-b: Esc キーを押した場合、変更を破棄して元の名前に戻す
- 並び替え：ステータス列ヘッダーをドラッグしてボード内で左右に並び替える → `PATCH /api/statuses/{id}/move`

---

### UC-08: ステータスを削除する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | ステータスが2件以上存在する（最後の1件は削除不可） |
| 事後条件 | ステータスが削除され、所属していた問い合わせは指定された移動先ステータスに移っている |

**基本フロー**
1. ユーザーがステータス列ヘッダーの削除ボタンをクリックする
2. 確認ダイアログが表示される（「このステータスを削除しますか？所属する問い合わせの移動先を選択してください」）
3. ユーザーが移動先ステータスを選択し「削除」を押す
4. システムが `DELETE /api/statuses/{id}?move_to={otherId}` を呼び出す
5. ステータス列がボードから消え、問い合わせは指定した列に移動している

**代替フロー**
- 3-a: ユーザーが「キャンセル」を選択した場合、削除を行わずダイアログを閉じる
- 1-a: 該当ステータスに問い合わせが0件の場合、移動先選択は不要（単純削除）
- 1-b: ステータスが最後の1件の場合、削除ボタンを非活性にする

---

### ~~UC-09: 優先度を作成する~~ ❌ MVP スコープ外

優先度は **3 段階固定運用（高/中/低）** とするため、追加機能は実装しない。`level` の UNIQUE 制約と 1..3 の範囲制約により、新規追加は DB 設計上も想定外。将来段階数を増やす場合は migration / model 制約 / 優先度アイコンの再設計を伴う別 Issue として扱う。

---

### ~~UC-10: 優先度を編集する~~ ❌ MVP スコープ外

3 段階固定の前提では「高/中/低 の name や color の編集」しか余地がなく、UI を提供する実用的価値が薄いため見送る。色やラベルの調整が必要になった場合は seed / マイグレーションで対応する。

---

### ~~UC-11: 優先度を削除する~~ ❌ MVP スコープ外

3 件固定で常に Inquiry がひもづく前提（`inquiries.priority_id` は NOT NULL、FK は RESTRICT）のため、削除を許す UI は提供しない。

---

### UC-12: ラベルを作成する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | 設定画面または InquiryModal が開いている |
| 事後条件 | 新しいラベルが作成され、ラベル一覧に表示されている |

**基本フロー**
1. ユーザーがラベル作成フォームに名前と HEX カラーを入力する
2. 作成ボタンをクリックする
3. システムが `POST /api/labels` を呼び出す
4. 新しいラベルがラベル一覧に追加される

**代替フロー**
- 1-a: 名前が空の場合、作成ボタンを非活性にする
- 1-b: カラーコードが HEX 形式でない場合、作成ボタンを非活性にする

---

### UC-13: 問い合わせにラベルを付与する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | ラベルが1件以上存在する、InquiryModal が開いている |
| 事後条件 | 問い合わせに選択したラベルが追加されている |

**基本フロー**
1. ユーザーが InquiryModal 内のラベル一覧から未選択のラベルをクリックする
2. システムが `POST /api/inquiries/{id}/labels/{labelId}` を呼び出す
3. ラベルがチェック済みになり、カードサムネイルにバッジが表示される

---

### UC-14: 問い合わせからラベルを解除する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | 問い合わせにラベルが1件以上付与されている、InquiryModal が開いている |
| 事後条件 | 問い合わせから選択したラベルが外れている |

**基本フロー**
1. ユーザーが InquiryModal 内のラベル一覧から選択済みのラベルをクリックする
2. システムが `DELETE /api/inquiries/{id}/labels/{labelId}` を呼び出す
3. ラベルのチェックが外れ、カードサムネイルからバッジが消える

---

### UC-15: ラベルを削除する

| 項目 | 内容 |
|------|------|
| アクター | ユーザー |
| 事前条件 | ラベルが1件以上存在する |
| 事後条件 | ラベルが削除され、付与されていた問い合わせからも外れている |

**基本フロー**
1. ユーザーが設定画面のラベル一覧で削除ボタンをクリックする
2. 確認ダイアログが表示される（「このラベルを削除しますか？」）
3. ユーザーが「削除」を選択する
4. システムが `DELETE /api/labels/{id}` を呼び出す
5. ラベルが一覧から消え、付与されていた問い合わせからも外れる

**代替フロー**
- 3-a: ユーザーが「キャンセル」を選択した場合、削除を行わずダイアログを閉じる
